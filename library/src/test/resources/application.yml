spring:
  shell:
    interactive:
      enabled: false
  data:
    mongodb:
      database: library
      port: 0

mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.library.mongock.changelog
  mongo-db:
    write-concern:
      journal: false
    read-concern: local